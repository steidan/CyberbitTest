namespace Users
{
    internal class User
    {
        // Thought about it; putting aside conventions, consistency is the most important. Thus, why to use lower case letters ?
        internal string Name { get; set; }
    }
}