*   The servers aren't restful according to the protocol but support RPC over HTTP. Done so because
    it's popular and convenient and couldn't get an answer from you on time regarding to that matter.
*   Assumed that there's no need to handle cases of concurrency, and thus no atomic operations and transactions where needed
    if needed.
